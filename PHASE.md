# AI Agent Pro v2.48.0 — Production (Real Runtime)

Phases 1–48. No demo providers, no fake API keys, no simulated tool results.

## Run
```bash
npm install
npm start
# http://localhost:3000
```

Add a real provider API key in Settings before agent chat.

## Notes
- Digital Twin "simulate" = real pre-deploy checks (health, files, runtime smoke), not fake success.
- Workspace tools execute on disk for real.
